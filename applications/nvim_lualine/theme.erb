-- Lualine theme for Solunized
-- Auto-generated by generate_themes.rb — do not edit by hand.

local palettes = {
<% themes.each do |variant, theme_data| -%>
  ["<%= variant %>"] = {
<% theme_data["colors"].each do |name, hex| -%>
    <%= name.ljust(12) %> = "<%= hex %>",
<% end -%>
  },
<% end -%>
}

local function get_variant()
  if vim.o.background == "dark" then
    if vim.g.solunized_dark_variant == "black" then return "black" end
    return "dark"
  else
    if vim.g.solunized_light_variant == "white" then return "white" end
    return "light"
  end
end

local p = palettes[get_variant()]

return {
  normal = {
    a = { fg = p.bg_0, bg = p.blue,    gui = "bold" },
    b = { fg = p.fg_1, bg = p.bg_2 },
    c = { fg = p.fg_0, bg = p.bg_1 },
  },
  insert = {
    a = { fg = p.bg_0, bg = p.green,   gui = "bold" },
    b = { fg = p.fg_1, bg = p.bg_2 },
    c = { fg = p.fg_0, bg = p.bg_1 },
  },
  visual = {
    a = { fg = p.bg_0, bg = p.magenta, gui = "bold" },
    b = { fg = p.fg_1, bg = p.bg_2 },
    c = { fg = p.fg_0, bg = p.bg_1 },
  },
  replace = {
    a = { fg = p.bg_0, bg = p.red,     gui = "bold" },
    b = { fg = p.fg_1, bg = p.bg_2 },
    c = { fg = p.fg_0, bg = p.bg_1 },
  },
  command = {
    a = { fg = p.bg_0, bg = p.orange,  gui = "bold" },
    b = { fg = p.fg_1, bg = p.bg_2 },
    c = { fg = p.fg_0, bg = p.bg_1 },
  },
  inactive = {
    a = { fg = p.dim_0, bg = p.bg_1,  gui = "bold" },
    b = { fg = p.dim_0, bg = p.bg_1 },
    c = { fg = p.dim_0, bg = p.bg_0 },
  },
}
